# Preferences Notes

User-specific active provider preference is stored in:
- `~/.lucy/preferences.json`

This is separate from provider-state.json so runtime preferences and provider metadata can evolve independently.
