# Provider State Notes

Current persisted state is local and user-specific.

File:
- `~/.lucy/provider-state.json`

Use it to track:
- whether a provider was connected
- which models were discovered
- which main default model the user selected
